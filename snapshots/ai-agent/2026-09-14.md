# Snapshot 2026-09-14

对照：`snapshots/ai-agent/2026-09-11.md`  
规则：记录当前事实。日报 / Diff 只报相对上一份快照的**新增能力**。  
窗口：2026-09-11 → 2026-09-14（周一 cron；中间无新产品接环）。

## as-studio-agent

- 生产宿主仍是 **Android Studio Quail 4 | 2026.1.4 Stable**。
- 预装 23 个官方 Android skills；自定义目录仍是 `.agents/skills` / `.android-studio/skills`。
- Studio skills 页 Last updated **2026-09-11**（相对 09-11 快照：文档日期前进；路径与触发方式未变）。
- AQI Fix with AI、LeakCanary Fix with Agent、并行 Agent Tab：未变。
- Studio MCP 仍 **只支持 HTTP/SSE**，**不支持 stdio**（MCP 页仍 2026-08-31）。Crashlytics / Maestro 不能装进 Studio Agent。
- Preview 页 Last updated 仍 **2026-09-09**。Rabbit 1 仍为 Canary；Studio Labs 唯一 AI 条目仍是 Journeys。
- Gemma 4 本地、Model Assignment、Gemini Enterprise：配额/治理，不是新闭环。

## as-rabbit1-migrate

- 能力边界未变：iOS / Flutter / RN → 规划 → Kotlin/Compose → **编译检查**。
- Verify 仍停在编译，无设备 / UI / 行为等价。
- 无新 Canary 版本叙事，无独立 deep-link 文档页。

## crashlytics-mcp

- 仍 **Experimental**（无 SLA）。
- 专用页 Last updated **2026-09-11**（09-11 快照为 2026-09-10）。
- 通用 Firebase MCP 页 Last updated **2026-09-10**。
- 工具面未扩环：`get_issue` / `list_events` / `batch_get_events` / `get_report` / `create_note` / `delete_note` / `list_notes` / `update_issue`。
- MCP resource 仍是 `crashlytics_investigations_guide` / `issues_guide` / `reports_guide`。
- 无 ANR 专用工具。Studio 侧继续走 AQI。

## android-cli-skills

- Android CLI 文档 Last updated **2026-09-11**（09-11 快照为 2026-09-01）。
- **命令面无新一等命令。** 仍无 `android build`；编译仍走 Gradle / `android studio` 桥。
- `android studio *` 仍 Preview。
- GitHub `android/skills` main：仍 **24** 个 `SKILL.md`。最近提交仍是 **2026-09-07** `Bump plugin version to v1.0.11`。09-07 之后无新 commit、无新技能文件。
- `android-profiler` 仍是 recording + analysis 编排器（Perfetto SQL / `trace_processor`）。

## apa-perfetto-agent

- APA AI 页 Last updated 仍 **2026-09-04**。
- 闭环未变：已有 `.perfetto-trace` → skill 路由 → Perfetto SQL → 本地 `trace_processor` → CSV / 根因。
- **仍不**自动改业务代码，**仍不**自动再采同一指标。

## journeys

- 文档仍 Last updated **2026-08-31**。
- 能力边界未变（无多指/长按/双击/旋转/记忆/计数/条件）。
- Studio Labs 仍只有这一条实验 AI 功能。

## maestro-mcp

- CLI 仍 **2.10.0**（2026-08-31）。无 2.11+。
- 当前公开 MCP 工具表：`list_devices` / `inspect_screen` / `take_screenshot` / `run` / `cheat_sheet` / `list_cloud_devices` / `run_on_cloud` / `get_cloud_run_status` / `open_maestro_viewer`。
- `describe_cloud_run` 仍不在工具表里（与 09-11 一致：以 2.7.0 changelog / 博文为准，不算本周新增）。
- 跑测 artifact 仍带 `crash-report.txt` / Android `anr-report.txt`。

## cursor-bugbot / cursor-cloud-agent / claude-code / android-bench / github-aw

- Bugbot：无新的 Android 领域规则或官方 Android review 引擎。
- Cloud Agent：官方文档仍是仓库 VM + MCP + computer use。**无官方 Android 模拟器 / 可用 nested KVM。** 论坛 2026-09-10 仍报 `KVM_CREATE_VCPU` 内核 BUG。Self-hosted machines（2026-09-02，早于上份快照）可自带硬件，不是一等 Android 模拟器产品。
- Claude Code Firebase plugin / Android skills：无新官方 Android 闭环。
- Android Bench：仍 **2026-08-11**。Top 仍 Opus 5 91.8 / Fable 5 90.9 / GPT 5.6 Sol 90.8。
- GitHub Agentic Workflows：无 Android 专用引擎或权限模型变化。Java/Gradle 仍靠 runner 自备 JDK + `./gradlew`。

## gbox-mcp / androidbuild-mcp / gradle-mcp / jenkins-ai

- 社区拼装层：无新的可进入团队流程的官方闭环。
- Jenkins：`jenkinsci/ai-agent` 是通用「在 Pipeline 里跑 Claude/Codex/Cursor」步骤，**不是** Android Crash/ANR/设备/Play 一等闭环。维持 Hold。

## 明确仍未形成闭环

- 生产 ANR 一键修复并复现。
- 性能：改代码后自动再采同一 Perfetto 指标。
- Cloud Agent 官方 Android 模拟器（可用 KVM）。
- Jenkins 官方 Android Agent。
- 架构分析的自动验证。
- Studio Agent 直接挂 stdio MCP（Crashlytics / Maestro）。
- 官方 `android build`。
