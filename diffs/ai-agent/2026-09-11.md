# Diff 2026-09-11

对比：`snapshots/ai-agent/2026-09-10.md` → `snapshots/ai-agent/2026-09-11.md`  
规则：只列**新增能力**（含昨日快照漏记、但今天核对后确认可进流程的能力）。不重复 Crash AQI / Leak / 官方 24 skills / Journeys 基线。

## 新增能力

### 1. Quail 4 把官方 skills 预装进生产 IDE

- 稳定版 Agent 不再依赖工程师先跑 `android skills add`。
- 23 个预装 skill 按 prompt 自动触发；团队自定义 skill 走 `.agents/skills`。
- **省时间点**：少一次全员环境配置；AGP 9 / Nav3 / edge-to-edge / R8 等机械迁移在 Studio 内即可启动。
- 不是新闭环类型，是 Adopt 宿主从 Quail 2 换到 Quail 4。

### 2. Performance 分诊闭环（android-profiler → Perfetto SQL）

昨日快照把 `android-profiler` 写成「只接地」。今天核对官方 APA AI 页（2026-09-04）和 skill 编排器后，应记为：

```text
卡顿 / 启动慢 / 内存
 ↓
APA 或 Profiler 录 system trace
 ↓
android-profiler（analysis orchestrator）
 ↓
生成并执行 Perfetto SQL（trace_processor）
 ↓
根因报告（可复现的查询）
```

- **有 Verify 的是诊断，不是修复。** 改代码 + 再采同一指标仍未接上。
- 这是目前唯一接到「够格的性能工程产物」（`.perfetto-trace`）而不是聊天摘要的官方路径。

### 3. Rabbit 1 跨平台工程迁移（Canary）

```text
iOS / Flutter / React Native 工程
 ↓
New Project Wizard 迁移助手
 ↓
规划 + 代码转换成 Kotlin/Compose
 ↓
编译检查
 ↓
给人做测试与验收
```

- 完整度：Modify Code + 编译 Verify。无设备 / UI / 行为等价。
- 只对「有一份要迁到原生 Android 的源工程」的团队有意义。

### 4. Maestro：Cloud 失败调试 + 跑测期 Crash/ANR 产物

昨日只写了「写 YAML → 跑 → 自修」。补记：

- `describe_cloud_run`：失败 Cloud run → 录像 / log / hierarchy → 解释并改 YAML 或业务代码。
- 本地与 Cloud artifact 带 `crash-report.txt`、Android `anr-report.txt`。
- 这是 **测试会话内** 的 Crash/ANR 输入，不是 Crashlytics 生产 ANR。

## 明确不算新增能力

- Crashlytics 文档日期 09-09 → 09-10：仍 Experimental，工具未扩环。
- Android CLI 文档仍 09-01；`studio open-file` / `studio check` 是漏记旧命令。
- android/skills 无新 SKILL.md（24，2026-09-07 仅 plugin 版本）。
- Android Bench、Journeys、Cloud 模拟器、Jenkins：无变化。
- Gemma 4 本地模型、Model Assignment、Gemini Enterprise：配额/治理，不进流程。
- Studio MCP Marketplace：HTTP only，不能把 Crashlytics/Maestro 接进 Studio。
