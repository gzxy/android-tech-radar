# Diff 2026-09-10

对比：无上一份快照（首次建档）。  
规则：只列**新增能力**。今日全部条目视为新增入库。

## 新增能力

### Android Studio Agent 进入 Crash / Leak 修复位

- AQI `Fix with AI`：生产 Crash → 读本地源码 → 出计划 → 批准后改代码并 verify。
- Profiler LeakCanary `Fix with Agent`：泄漏轨迹 → 解释 retain 链 → 写解除引用补丁。
- 并行 Agent Tab：UI 重构 / ProGuard / 文档可同时跑（同文件无隔离）。

### Crashlytics 数据成为 Agent 工具而不是控制台页面

- MCP 工具集 + `/crashlytics:connect`。
- Agent 可拉 issue/event、写 note、关 issue。
- 文档 2026-09-09 仍标 Experimental。

### 官方 Agent 工具面（Android CLI + skills）

- CLI：设备、布局树、截图坐标、知识库 `kb://`、Studio 语义分析与 Compose Preview 渲染。
- 24 个官方 SKILL.md，覆盖 AGP 9、Navigation 3、edge-to-edge、R8、XML→Compose、Play Billing、Intent 安全等「模型容易写错」的流程。
- 可安装到 Cursor / Claude Code / Codex / Gemini。

### UI 测试两条可执行路径

- Journeys：自然语言步骤，Gemini 看屏执行；可经 Android CLI 进 CI；不产确定性脚本。
- Maestro MCP：Agent 写 YAML、跑、自修、入库；CI 可脱离模型。

### Review / CI 仓库 Agent

- Bugbot：推前 `/review` + PR 自动审 + Autofix Cloud Agent。
- GitHub Agentic Workflows：CI 失败可自动分析并开修复 PR；可用 `GITHUB_TOKEN`。
- Cloud Agent：仓库级闭环成立；Android 模拟器官方未交付。

## 明确未形成闭环（故不记为能力）

- ANR 一键修复并复现。
- 性能（卡顿/启动）除 Leak 外的自动改代码 + 复测。
- Cloud Agent 无手扶跑 Android 模拟器。
- Jenkins 官方 Android Agent。
- 架构分析的自动验证（无测试契约则不可信）。
